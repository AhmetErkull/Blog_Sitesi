@model List<Tag>

<div>
    <h3>Etiketler</h3>
    <div>
        @foreach (var item in Model)
        {
            <a href="#" class="btn btn-outline-secondary btn-sm me-1 mb-1">@item.TagName</a>
        }
    </div>
</div>